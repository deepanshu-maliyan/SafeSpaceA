import SwiftUI

struct AppColors {
    static let background = Color.black
    static let cardBackground = Color(red: 0.1, green: 0.1, blue: 0.15)
    static let accent = Color(red: 0.0, green: 0.8, blue: 0.8)
    static let secondaryAccent = Color(red: 0.8, green: 0.2, blue: 0.2)
    static let text = Color.white
    static let secondaryText = Color(white: 0.7)
    
    // Status colors
    static let success = Color.green
    static let warning = Color.yellow
    static let danger = Color.red
    static let info = Color.blue
} 